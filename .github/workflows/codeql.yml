# .github/workflows/codeql.yml
name: CodeQL

on:
  push:
    branches: [ master, main ]
  pull_request:
    branches: [ master, main ]
  schedule:
    - cron: '22 4 * * 1' # every Monday at 04:22 UTC

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language: [ cpp ]  # Only C/C++ – no Rust

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # Install all the system dependencies the build actually needs
      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            autoconf \
            gcc \
            pkg-config \
            libpam0g-dev \
            libcairo2-dev \
            libxcb1-dev \
            libxcb-xinerama0-dev \
            libxcb-randr0-dev \
            libxcb-xrm-dev \
            libxcb-util0-dev \
            libxcb-image0-dev \
            libxcb-keysyms1-dev \
            libxcb-composite0-dev \
            libev-dev \
            libx11-xcb-dev \
            libxcb-dpms0-dev \
            libgif-dev  # Required for GIF support

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: ${{ matrix.language }}
          queries: security-and-quality

      # Explicitly build the project (CodeQL traces the compilation)
      - name: Build project
        run: |
          ./autogen.sh
          ./configure
          make -j$(nproc)

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3
